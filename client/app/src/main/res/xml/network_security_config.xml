<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <!-- (Since server is host locally on the machine) -->
        <!-- Enables runs on Simulators/Emulators -->
        <domain includeSubdomains="true">10.0.2.2</domain>
        <!-- Add the Machine Specific IP to enable Running on Devices connected to the same WIFI -->
        <domain includeSubdomains="true">192.168.43.146</domain>
        <!-- Also Update base_url dev.properties -->
    </domain-config>
</network-security-config>
